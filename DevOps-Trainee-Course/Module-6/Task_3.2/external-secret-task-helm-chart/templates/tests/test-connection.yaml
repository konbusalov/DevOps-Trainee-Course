apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "external-secret-task-helm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "external-secret-task-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "external-secret-task-helm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
