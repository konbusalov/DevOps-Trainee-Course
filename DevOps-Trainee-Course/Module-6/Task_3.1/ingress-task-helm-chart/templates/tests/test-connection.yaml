apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingress-task-helm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ingress-task-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingress-task-helm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
